# Session Handoff — 2026-06-26 17:34 CDT

_Authoritative current-state bootstrap for a new session. Read this first, then the project `CLAUDE.md` for how the code works. Supersedes earlier dated handoffs (kept for history)._

## TL;DR — where the project is
- **Atelier Studio**: Next.js 16 App Router chat app, a **Claude.ai-style clone for construction work**. **Claude = brain** (chat, web search, tools); **Gemini = senses** (image gen + embeddings + internal housekeeping). Supabase Postgres + pgvector via Drizzle. Deployed on Vercel. Single-password access gate (live).
- **Everything shipped to `master`, GitHub-released, CI green (both releases this session).** Current version **v4.31.1**. Working tree clean.
- **No half-done code. One open _decision_** (not work): whether to build the Tavily feature — research is done, recommendation below, nothing committed.
- **407 tests pass** (was 366 at the last handoff). Gate every tag: `npm run typecheck` (0) · `npm run lint` (**0 errors, 26 baseline warnings** — one fewer than the long-standing 27) · `npm run build` · `npm test`. E2E (`npm run test:e2e`) runs **in CI only** (gate-off, ephemeral pgvector) — not locally, because `.env.local` has the gate ON and `DATABASE_URL` points at **production** Supabase.

## What shipped this session (newest first)
- **v4.31.1 — Fix: persona selection sticks before a chat exists.** Root cause (systematic-debugging): since chats are created **lazily on first send** (v4.30.2), the default app state has **no `activeChatId`**, and `handleSaveSystemPrompt` early-returned in that case **before** `setCurrentSystemPrompt` — so picking a named persona (Coding, Deep Analysis, …) silently did nothing and the chip stayed on **General Assistant**. Fix: update the composer state locally when there's no chat yet (persisted to the chat on first send), **and** the standalone (quick-chat) deferred-create now persists `currentSystemPrompt` (the project branch already did). Regression test `e2e/persona-selection.spec.ts` (CI-verified red→green). `page.tsx` only.
- **v4.31.0 — Code-review hardening: security, correctness & robustness.** A structured **7-dimension code review** (9 module reviewers → every finding adversarially verified against source) surfaced **34 confirmed findings** (4 Major · 20 Minor · 10 Nitpick; **no Critical**). All fixed across **6 themed batches**, each gated + committed; +3 "uncertain" investigated (1 fixed, 2 confirmed non-issues). Highlights:
  - **Security:** login throttle no longer bypassable by spoofing `X-Forwarded-For` (keys on `x-real-ip`/rightmost XFF); document **replace flow re-validates** type/size; embedded file content can't forge the `FILECONTENT`/`FILES` sentinels; `'unsafe-inline'` CSP tradeoff **documented** (single-user + opaque-origin artifact iframe); light input bounds on persisted free-text.
  - **Correctness:** **copy-code button copies its own block** (was always the first on the page); **first-message-in-a-new-chat no longer flickers/drops** (stale-load guards + skip-once + assistant-save dedup); **artifact delete sweeps every version's Storage object** (was orphaning blobs); **markdown link URLs survive export** (docx hyperlinks, pdf/pptx `text (url)`); RAG: message-retrieval failure no longer nulls doc context, rerank de-dups, edit-route rejects shape mismatch (422), classify pinned to Flash, inline-code renders under react-markdown v10.
  - **Tests:** ~30 new/extended, including **first-ever access-gate middleware coverage** (`tests/unit/middleware.test.ts` — matcher-bypass + branch logic), `sanitizeStorageName` traversal, `embed-route` validation, `CodeBlock` copy.
  - Plan: `~/.claude/plans/create-plan-to-make-quirky-neumann.md` (the approved 6-batch plan). Full per-item detail in `CHANGELOG.md`.
- **Tavily research (NO code — decision pending).** Deep-dive done (verified web research + codebase integration mapping). **Verdict: the app already has web search twice over** (Claude native `web_search_20250305` + Google grounding), so Tavily **Search is redundant**; the net-new value is **Extract (URL→clean markdown) and Crawl/Map (site→RAG)** feeding the existing `chunk→embed→pgvector` pipeline. **Recommendation: build "ingest a URL/site into a project" (Design B, ~M effort, no migration — reuses `documents/process` tail verbatim); optionally a small `web_extract` chat tool (Design A, S); skip Tavily-search-as-tool and skip replacing native web search.** Key facts: official `@tavily/ai-sdk` v0.5.0 (peers `ai ^5||^6`, `zod ^4`, factories take `apiKey?` → fits DB-first `settings.ts`); free tier 1,000 credits/mo (no card), PAYGO $0.008/credit, Extract ≈ 1 credit / 5 URLs. ⚠️ **Nebius is acquiring Tavily** ($275M, announced 2026-02-10) — re-verify pricing/roadmap before committing. Also did a small `/init` doc-accuracy pass on `CLAUDE.md` (migration range `0000`→`0011`, table list) earlier in the session.

## Live infrastructure (unchanged this session)
- **Supabase** project ref `evhgyudnjyryayazupgh`. Migrations `0000`–`0011` applied; drizzle ledger in sync (a future `drizzle-kit migrate` is a clean no-op — **no new migrations this session**). RLS on all tables. Bucket `atelier-files` (private, 200MB).
- **Vercel**: repo linked to project **`atelier-ai`** (prod alias **atelier-ai-app.vercel.app**). Auto-deploys on push to `master` (both v4.31.0 and v4.31.1 deployed). CLI installed + authed.
- **Access gate LIVE** (`APP_ACCESS_PASSWORD` + `AUTH_SECRET`). Guide: `docs/AUTH.md`.

## Working cadence (the user expects this)
- Act as **Sr Fullstack Engineer**; make decisions, don't stall on small safe steps. New scope → brainstorm → spec (`docs/specs/`) → phased plan → execute (subagent-driven / batched, gate each) → ship. Large reviews/research → multi-agent workflows with adversarial verification.
- Solo dev → branch off `master`, commit per verified batch, then on the user's go: merge (ff) → version bump + `CHANGELOG` → annotated tag `vX.Y.Z` → push `master` + tag → `gh release create` → watch CI → Vercel auto-deploys. Commit trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **User-gated & outward-facing**: production pushes/releases are **confirmed each time** (last turn's release approval does not auto-extend). Do NOT run `vercel env pull` / `vercel dev`. Live e2e/manual smoke is blocked locally (gate on + prod DB) → rely on CI e2e + the user's in-app check.

## Next candidates (nothing required)
- **Tavily — DECISION PENDING.** If yes: brainstorm → spec → plan on **Design B** ("Add from URL / crawl a docs-site into a project" → RAG), optionally the S-effort **`web_extract`** chat tool. Start on Tavily's free tier behind a `getTavilyApiKey()` gate (mirror Gemini/Anthropic keys + `SENSITIVE_KEYS`). Full analysis is in this session's chat (products/pricing/integration/competitive-delta + exact file plug-in points: `image/tool.ts` pattern, merge at `chat/route.ts:116-124`, reuse `documents/process/route.ts:50-159`).
- **Carryover from the 2026-06-25 handoff (still open):** finish `page.tsx` decomposition (`useDialogs` reducer for the 13 dialog flags + `useChatPersistence` for the `onFinish` pipeline); **brand cleanup** — migrate the ~250 hardcoded `white/X`·`black/X` overlay utilities to semantic tokens; **CSP nonce pipeline** to drop `script-src 'unsafe-inline'` (v4.31.0 only *documented* the tradeoff in `next.config.ts` — the nonce hardening is still the open option); verify **xlsx artifact editing** round-trips through the workspace edit route.
- **Deferred / separate projects:** React/JSX artifacts (bundler); full per-user auth (Clerk + `ownerId`).

## Quick links
- `CLAUDE.md` (source of truth for how the code works) · `CHANGELOG.md` (per-release detail) · `docs/AUTH.md`.
- This session's plan: `~/.claude/plans/create-plan-to-make-quirky-neumann.md`. Memory index: `~/.claude/projects/.../memory/MEMORY.md`.
- GitHub releases: `v4.31.0`, `v4.31.1` at github.com/DanielTso/atelier_ai_gpt/releases.
